# TODO: Implement Getters and Setters for Student Class

- [x] Update the `__init__` method to set private attributes (e.g., `self._first_name` instead of `self.first_name`)
- [x] Add `@property` getters for all instance attributes: `first_name`, `last_name`, `age`, `gender`, `student_id`, `course`, `instructor`
- [x] Add `@setter` setters for `first_name`, `last_name`, `age`, `gender` with existing validations (type checks, age > 18, gender normalization)
- [x] Add `@setter` setters for `student_id`, `course`, `instructor` without validations for consistency
- [x] Test the class by instantiating a Student object and verifying getters/setters work with validations
